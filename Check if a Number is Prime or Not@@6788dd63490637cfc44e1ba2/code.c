#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    integer=(int)a;
    for(2<a<n-1)
    if((a>1 && a%1==0) || (a%2!=0 && a%a==0)){
        printf("Prime");
    }
    else{
        printf("Not Prime");
    }
    return 0;
}